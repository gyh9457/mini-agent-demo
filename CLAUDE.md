# 项目规则

## 语言

始终使用中文回复。包括代码注释、commit message 描述、计划文档、对话交流。

代码中的变量名、函数名、类型名保持英文。

## Plan 编写规则

写实现计划（superpowers:writing-plans）时，按以下规则控制详细程度：

**需要完整代码的部分：**
- 核心算法和业务逻辑（agent 循环、状态机、解析器）
- 有微妙坑的 API 调用（流式 chunk 拼接、增量合并）
- 测试用例（定义了"正确"的标准）
- 数据 schema 和类型定义

**只需接口约束 + 行为描述的部分：**
- UI 组件、CSS 样式
- 框架 boilerplate（Next.js 布局、路由配置）
- 标准 CRUD、通用胶水代码
- 配置文件（tsconfig、vitest config）

对于后者，写清楚：文件路径、输入输出接口、关键行为约束。不要写完整实现代码。

**判断标准：** 如果这段代码让一个有经验的开发者写，他会觉得"闭眼都能写"，那就不要展开完整代码。

## Git 提交规则

- 不要在 commit message 中添加 `Co-Authored-By` 或任何 AI 署名行
